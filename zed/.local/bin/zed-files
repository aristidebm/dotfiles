#!/usr/bin/env bash

function zed_files() {
  # We use rg --files here so we can respect ignore files
  local selected=$(rg --files --hidden --glob '!.git/' | fzf --reverse --ansi --multi \
      --prompt="Files > ")
  if [[ -n "$selected" ]]; then
      zed $selected
  fi
}

zed_files
