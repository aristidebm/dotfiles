# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# # Replace C-b (default) prefix by C-space
# set -g prefix C-Space
# bind C-Space send-prefix
# unbind-key C-b

# activate tmux extended keys (needed by pi-coding agent)
set -g extended-keys on
set -g extended-keys-format csi-u

# # use vi keybinding in copy mode
set -g mode-keys vi

# # source: https://github.com/salcode/ironcode-vim/issues/91
# # Set focus-events on (important for Neovim autoread)
# set -g focus-events on
#
# # Make neovim escaping from insert mode a little more quicker
# set -sg escape-time 0
#
#
# # Force 256color terminal (with true color capabilities)
# set -g default-terminal "tmux-256color"
# set -ga terminal-features ",*-256color:Tc"
#
# set -g extended-keys on
#
# # fallback to another session when the current is killed
# # set -g detach-on-destroy off
#
# # destroy a session when there is no window attached to it
# # set -g destroy-unattached on
#
# # enable mouse support
# set -g mouse on
#
# # change the color of copy mode selection
# # set -g mode-style "fg=default,bg=default,reverse"
# set -g mode-style "fg=#8BB7C7,bg=#292C3C,reverse"
# set -g message-style "fg=#8BB7C7,bg=#292C3C,reverse"
#
# # status bar structure customization
set -g base-index 1
# set -g pane-base-index 1
# set -g status-position bottom
# set -g status-left-length 60
# set -g status-left "[#S]" # or "#[fg=#1C1F2A] #[bg=#E1ACFF] #S"
# # set -g status-right "%H:%M |#(acpi | head -n 1 | cut -d, -f2)"
# set -g status-right "%a %H:%M"
# set -g status-justify centre
# set -g window-status-format '#I:#W'
# set -g window-status-current-format '#[fg=#61808B] *#I:#W'
#
# # status bar looking customization
# set -g status-style "fg=white,bg=default bold"
#
# set -g window-style bg=default
#
# # panes looking
# set -g pane-border-style "fg=#1C1F2A"
# set -g pane-active-border-style "fg=#61808B"
#
# # Windows keybindings
# # bind Enter new-window
# # bind j select-window -t 1
# # bind k select-window -t 2
# #bind l select-window -t 3
# #bind \; select-window -t 4
#
# # Panes keybindings
# bind -r / split-window -h -c "#{pane_current_path}"
# bind -r . split-window -v -c "#{pane_current_path}" -l '30%'
# bind -r h select-pane -L
# bind -r l select-pane -R
# bind -r j select-pane -D
# bind -r k select-pane -U
# # bind -r h select-pane -t :.+
# # close all other panes except the current one
# # o stands for [O]nly like in vim <C-w><C-o>
# bind -r o kill-pane -a
# bind -r K swap-pane -U
# bind -r J swap-pane -D
#
# # Copy mode keybindings
# bind -T copy-mode-vi v send-keys -X begin-selection
# bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
#
# if-shell "[ $XDG_SESSION_TYPE = 'tty' ]" {
#     bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -se c -i'
#     bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'xclip -se c -i'
# }
#
# if-shell "[ $XDG_SESSION_TYPE = 'wayland' ]" {
#     bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'wl-copy'
#     bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'wl-copy'
# }
#
# # Use incremental search in copy mode
# bind -T copy-mode-vi / command-prompt -i -p "search down" "send -X search-forward-incremental \"%%%\""
# bind -T copy-mode-vi ? command-prompt -i -p "search up" "send -X search-backward-incremental \"%%%\""
# # text objects [source](https://www.reddit.com/r/vim/comments/12eg832/is_there_a_way_to_have_vim_objects_in_tmux/)
# bind -T copy-mode-vi i switch-client -T copyModeMultiKey_i
# bind -T copyModeMultiKey_i w send-keys -X select-word
# bind -T copyModeMultiKey_i W send-keys -X clear-selection \; send-keys -X previous-space \; send-keys -X begin-selection \; send-keys -X next-space-end
# bind -T copyModeMultiKey_i b send-keys -X clear-selection \; send-keys -X jump-to-backward '(' \; send-keys -X begin-selection \; send-keys -X jump-to-forward ')'
# bind -T copyModeMultiKey_i ( send-keys -X clear-selection \; send-keys -X jump-to-backward '(' \; send-keys -X begin-selection \; send-keys -X jump-to-forward ')'
# bind -T copyModeMultiKey_i ) send-keys -X clear-selection \; send-keys -X jump-to-backward '(' \; send-keys -X begin-selection \; send-keys -X jump-to-forward ')'
# bind -T copyModeMultiKey_i B send-keys -X clear-selection \; send-keys -X jump-to-backward '{' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '}'
# bind -T copyModeMultiKey_i \{ send-keys -X clear-selection \; send-keys -X jump-to-backward '{' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '}'
# bind -T copyModeMultiKey_i \} send-keys -X clear-selection \; send-keys -X jump-to-backward '{' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '}'
# bind -T copyModeMultiKey_i [ send-keys -X clear-selection \; send-keys -X jump-to-backward '[' \; send-keys -X begin-selection \; send-keys -X jump-to-forward ']'
# bind -T copyModeMultiKey_i ] send-keys -X clear-selection \; send-keys -X jump-to-backward '[' \; send-keys -X begin-selection \; send-keys -X jump-to-forward ']'
# bind -T copyModeMultiKey_i < send-keys -X clear-selection \; send-keys -X jump-to-backward '<' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '>'
# bind -T copyModeMultiKey_i > send-keys -X clear-selection \; send-keys -X jump-to-backward '<' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '>'
# bind -T copyModeMultiKey_i ` send-keys -X clear-selection \; send-keys -X jump-to-backward '`' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '`'
# bind -T copyModeMultiKey_i \' send-keys -X clear-selection \; send-keys -X jump-to-backward "'" \; send-keys -X begin-selection \; send-keys -X jump-to-forward "'"
# bind -T copyModeMultiKey_i \" send-keys -X clear-selection \; send-keys -X jump-to-backward '"' \; send-keys -X begin-selection \; send-keys -X jump-to-forward '"'
# bind -T copyModeMultiKey_i l send-keys -X clear-selection \; send-keys -X back-to-indentation \; send-keys -X begin-selection \; send-keys -X end-of-line \; send-keys -X cursor-left \; send-keys -X other-en

# bind f send-keys 'source ~/.local/bin/file-switcher.sh' Enter
# -b is used to avoid fzf-tmux locking fzf
# source https://github.com/junegunn/fzf/issues/1841#issuecomment-580975759
# The || true is used to make the command always succeed otherwise run-shell will try
# to print the error message
bind e run -b "
    sesh connect $(sesh list | fzf-tmux -d 50% \
		--no-sort --ansi --layout=reverse --prompt '> ' \
		--header ' ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
		--bind 'tab:down,btab:up' \
		--bind 'ctrl-a:change-prompt(> )+reload(sesh list)' \
		--bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t)' \
		--bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c)' \
		--bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z)' \
		--bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
		--bind 'ctrl-d:execute(tmux kill-session -t {})+change-prompt(> )+reload(sesh list)') || true"

bind g setw synchronize-panes
bind v send-keys 'vifm' Enter

# ccmux fleet management for AI agents
# pop open the ccmux picker from anywhere
bind-key a display-popup -E -w 95% -h 80% "ccmux"

# # toggle ccmux sidebars in all windows
# bind-key S run-shell "ccmux sidebar --toggle"

# Initialize TPM (keep this line at the very bottom of tmux.conf)
run -b '~/.config/tmux/plugins/tpm/tpm'
